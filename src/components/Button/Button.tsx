import React from 'react';

import ss from '@assets/react.svg';

const Button = () => {
  return (
    <div>
      <img src={ss} alt="react" />
      <button>Click me</button>
    </div>
  );
};

export default Button;
